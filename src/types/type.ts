export interface Type {
  product_type_id: number;
  code: string;
  name: string;
  status: string;
}

export interface SellType {
  sell_type_id: number;
  code: string;
  name: string;
}

export interface BuyType {
  buy_type_id: number;
  code: string;
  name: string;
}
